# 🧪 测试指南

## ✅ 服务器状态
- **开发服务器**: 正在运行 ✅
- **访问地址**: http://localhost:3000
- **状态码**: 200 OK

---

## 🔐 登录测试

### 管理员账号（从 .env.local）
```
用户名: xjl
密码: xjl
```

---

## 🤖 模型测试

### 测试 1: GPT-5.5
1. 登录后，点击左下角的模型选择器
2. 选择 "🤖 GPT-5.5"
3. 发送测试消息：`你好，请做个自我介绍`
4. 观察：
   - ✅ 流式响应是否正常
   - ✅ "正在思考..." 步骤是否显示
   - ✅ 流式光标是否闪烁
   - ✅ 完成后是否有完整响应

### 测试 2: Claude Opus 4.8
1. 切换模型到 "🧠 Claude Opus 4.8"
2. 发送测试消息：`请用中文介绍一下你自己`
3. 观察相同的功能点

---

## 🎨 UI 测试

### 深色科技感主题
- [ ] 背景是否有多层渐变网格效果
- [ ] 用户消息气泡是否有渐变（蓝紫色）
- [ ] AI 消息气泡是否有深色背景和边框
- [ ] 新建对话按钮是否有渐变效果

### 侧边栏
- [ ] 新建对话按钮：渐变背景 + 悬停效果
- [ ] 活跃会话：渐变边框 + 高亮
- [ ] 滚动条样式是否美观

### 动画效果
- [ ] 按钮点击有缩放反馈
- [ ] 菜单展开有淡入动画
- [ ] 消息发送后平滑滚动到底部

---

## 🧠 思考步骤测试

发送一个复杂问题，观察 AI 响应时：
```
请帮我规划一个为期3天的北京旅游行程，包括景点推荐、交通方式和预算估算
```

**期望效果：**
- ✅ 在 AI 开始回复时，显示思考步骤框
- ✅ "正在思考..." 文字 + 转圈动画
- ✅ 描述显示 "分析您的问题"
- ✅ 流式响应时有闪烁光标
- ✅ 完成后思考步骤消失

---

## 🖼️ 多模态测试（图片上传）

1. 点击输入框左侧的 📎 图标
2. 上传一张图片
3. 发送消息：`这张图片里有什么？`
4. 观察图片是否正确显示和处理

---

## 📱 响应式测试

### 桌面端 (>1024px)
- [ ] 侧边栏固定在左侧
- [ ] 聊天区域占据剩余空间
- [ ] 模型选择器在输入框上方

### 移动端 (<768px)
- [ ] 侧边栏变为抽屉式
- [ ] 左上角有汉堡菜单按钮
- [ ] 输入框字体至少 16px（防止 iOS 自动缩放）
- [ ] 虚拟键盘弹出时布局不错乱

---

## 🐛 错误处理测试

### 网络错误
1. 断开网络连接
2. 发送消息
3. 观察是否显示友好的错误提示

### Token 过期
1. 等待一段时间让 Token 过期
2. 发送消息
3. 观察是否提示重新登录

---

## 🔄 会话管理测试

### 新建会话
- [ ] 点击 "新建对话" 按钮
- [ ] 是否创建新的空白会话
- [ ] 标题是否自动生成

### 切换会话
- [ ] 点击历史会话
- [ ] 是否正确加载历史消息
- [ ] 是否滚动到消息底部

### 删除会话
- [ ] 悬停在会话上
- [ ] 点击删除按钮
- [ ] 是否显示确认对话框
- [ ] 删除后是否自动切换到其他会话

---

## ⚡ 性能测试

### 消息数量限制
- 当前限制：每个会话最多 20 条消息
- 测试：连续发送 25 条消息
- 观察：是否只保留最新的 20 条

### 内存管理
- 打开浏览器开发者工具 > Performance
- 发送多条消息
- 观察内存是否持续增长（应该保持稳定）

---

## 🎯 关键功能清单

- [x] Vercel AI SDK 流式响应
- [x] 多模型切换 (GPT-5.5 / Claude Opus 4.8)
- [x] 思考步骤可视化
- [x] 深色科技感 UI
- [x] 渐变效果和动画
- [x] 会话管理（新建/切换/删除）
- [x] 响应式布局
- [x] 图片上传和多模态
- [x] 消息懒加载
- [x] IndexedDB 持久化

---

## 🚨 已知问题

1. **流式解析兼容性**
   - AI SDK 返回的数据流格式为 `0:"text"`
   - 已在 `ChatInterface.tsx` 中处理

2. **中转 API 兼容性**
   - 确保你的 BASE_URL (`https://yunwu.ai/v1`) 支持 AI SDK 格式
   - 如果有问题，可能需要调整流式解析逻辑

3. **模型 ID 映射**
   - 确保中转 API 支持 `gpt-5.5` 和 `claude-opus-4-8` 这两个模型 ID
   - 如果不支持，需要在 `ModelSelector.tsx` 中修改为实际的模型 ID

---

## 📞 问题排查

### 问题：模型无响应
**解决：**
1. 检查 `.env.local` 中的 `API_KEY` 和 `BASE_URL`
2. 查看浏览器控制台是否有错误
3. 查看终端是否有 API 错误日志

### 问题：思考步骤不显示
**解决：**
1. 检查 `MessageItem.tsx` 是否正确导入 `ThinkingStep`
2. 检查 `isStreaming` prop 是否正确传递
3. 清除浏览器缓存重试

### 问题：样式错误
**解决：**
1. 运行 `npm run build` 重新构建
2. 清除 `.next` 缓存：`rm -rf .next`
3. 重启开发服务器

---

## 🎉 测试完成后

如果一切正常，你可以：
1. ✅ 部署到 Vercel
2. ✅ 配置生产环境变量
3. ✅ 开始添加 Tools 功能
4. ✅ 集成更多模型

祝测试顺利！🚀
